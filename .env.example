# Optional: durable idempotency across Vercel cold starts.
# Use either the Vercel KV names or the Upstash names.
KV_REST_API_URL=
KV_REST_API_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: forward each new event to the SMShadi bot's public webhook.
WEBHOOK_FORWARD_URL=
WEBHOOK_FORWARD_TOKEN=